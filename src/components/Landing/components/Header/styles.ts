import {makeStyles} from '@mui/styles';

export default makeStyles({
    container: {
        display: "flex",
        justifyContent: "start",
        width: 1366,
    },
    logoImg: {
        width: 60,
        height: 60,
    },
    test: {
        width: 60,
        height: 60,
    }
});
